server:
  # `practica` en el 8083 y `solucion` en el 8084: pueden correr a la vez.
  port: 8083

spring:
  application:
    name: lab02-di

logging:
  level:
    root: WARN
    cl.dgt: INFO
    org.springframework.boot.tomcat: INFO
