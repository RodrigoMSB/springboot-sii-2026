package cl.dgt.tramites.domain.tipo;

/** Quién es quién en la DGT. Carolina e Ignacio son usuarios, no contribuyentes. */
public enum RolUsuario {
    CONTRIBUYENTE,
    FUNCIONARIO,
    FISCALIZADOR
}
