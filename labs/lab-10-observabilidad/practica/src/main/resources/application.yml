server:
  port: 8101

spring:
  application:
    name: lab10-observabilidad

  datasource:
    url: jdbc:postgresql://localhost:55442/postgres
    username: postgres
    hikari:
      connection-timeout: 2000
      initialization-fail-timeout: -1

  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: classpath:db/migration

lab10:
  base:
    puerto: 55442

# Paso 1 · expón health, info y metrics, y sólo eso.
# Paso 4 · muestra el detalle del health.
# Paso 5 · enciende las sondas y mete la base en el grupo readiness.
# escribe aquí

logging:
  level:
    root: WARN
    cl.dgt: INFO
    org.springframework.boot.tomcat: INFO
    org.flywaydb: INFO
# Paso 2 · añade el traceId del MDC al patrón de consola.
# escribe aquí
