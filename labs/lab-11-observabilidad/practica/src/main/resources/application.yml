server:
  port: 8101

spring:
  application:
    name: lab11-observabilidad

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    locations: classpath:db/migration

lab11:
  base:
    puerto: 55442

# Paso 1 · expón health e info, y sólo eso.
# Paso 3 · muestra el detalle del health, enciende las sondas y mete la base
#          en el grupo readiness.
# escribe aquí

logging:
  level:
    root: WARN
    cl.dgt: INFO
    org.springframework.boot.tomcat: INFO
    org.flywaydb: INFO
# Paso 2 · añade el traceId del MDC al patrón de consola.
# escribe aquí
