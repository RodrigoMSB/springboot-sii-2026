server:
  # `practica` en el 8103 y `solucion` en el 8104. En el paso 5 corren los dos a la vez.
  port: 8104

spring:
  application:
    name: lab12-tareas-solucion

  threads:
    virtual:
      # Java 25: Tomcat y @Async pasan a hilos virtuales. Una línea. Paso 4.
      enabled: true

logging:
  level:
    root: WARN
    cl.dgt: INFO
    org.springframework.boot.tomcat: INFO
