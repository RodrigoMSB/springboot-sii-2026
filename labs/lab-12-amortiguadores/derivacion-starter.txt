# starter/ L12 vs su solucion/. El aviso que se evapora y los cuatro huecos.
#
# El pom, el compose y las dos migraciones vienen COMPLETOS: el lab no trata de buscar coordenadas
# en Maven Central ni de teclear un CREATE TABLE, trata de usar bien lo que ya esta. Igual que el
# recorte declarado del TODO_2 del Lab 11.
#
# ResilienciaConfig tampoco es un hueco: el CircuitBreaker viene declarado y configurado (umbral,
# ventana, HALF_OPEN) con su porque escrito. Lo que falta es USARLO — que es donde esta la leccion,
# y donde el test puede medir la diferencia en milisegundos.
#
# Archivos COMPARTIDOS que el starter rompe a proposito:
#
#   EscuchaDeFolios.java
#     TODO_1. EL CRIMEN. El aviso se manda AL AIRE: llamada directa al notificador, como en el
#     Lab 11. Con el servicio de avisos caido el mensaje se evapora, la API responde 201, nadie ve
#     un error y no queda forma de saber CUALES avisos se perdieron.
src/main/java/cl/dgt/tramites/application/evento/EscuchaDeFolios.java
#
#   ConsumidorDeAvisos.java
#     TODO_2. Procesa todo lo que llega, incluidos los duplicados. Y le van a llegar: "exactly once"
#     no existe. El metodo marcarComoProcesado(...) esta escrito y sin usar — mirar y marcar en una
#     sola sentencia atomica, igual que el candado del Lab 11.
src/main/java/cl/dgt/tramites/application/ConsumidorDeAvisos.java
#
#   AmqpConfig.java
#     TODO_3. La cola no declara adonde mandar a sus muertos: un mensaje que agota sus reintentos se
#     DESCARTA EN SILENCIO. Los beans de la DLQ estan escritos mas abajo; lo que falta es que la cola
#     principal sepa de ellos.
src/main/java/cl/dgt/tramites/config/AmqpConfig.java
#
#   TesoreriaAdapter.java
#     TODO_4. La llamada sale a la red SIEMPRE, aunque TESO lleve veinte minutos caido. El circuito
#     esta inyectado y sin usar, y el catch de CallNotPermittedException es hoy codigo muerto.
src/main/java/cl/dgt/tramites/infrastructure/teso/TesoreriaAdapter.java
