# =============================================================================
#  Perfil test — la suite automatizada
# -----------------------------------------------------------------------------
#  Nada de compose: el contenedor lo levanta Testcontainers desde el propio test,
#  y @ServiceConnection cablea el DataSource. Cada suite, su base limpia.
# =============================================================================
spring:
  docker:
    compose:
      enabled: false

dgt:
  jwt:
    secret: ${DGT_JWT_SECRET:clave-de-laboratorio-hs256-no-usar-en-produccion-2026}
